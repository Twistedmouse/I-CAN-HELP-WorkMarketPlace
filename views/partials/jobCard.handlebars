<div class="w-full bg-gray-100 to-blue-500 md:w-1/3 xl:w-1/4 p-2 m-2 flex flex-col rounded-2xl shadow-lg">

    <p class="text-l text-gray font-semibold pb-3"> JobName: {{job_name}}</p>
    <p class="text-l text-gray font-semibold pb-3">Price: ${{job_price}}</p>
    <p class="text-l text-gray font-semibold pb-3">Location: {{job_location}}</p>
    <p class="text-l text-gray font-semibold pb-3">Contact: {{user.email}}</p>
    <a href="/job/{{id}}">
        <button
            class='ml-auto bg-green-400 flex items-center gap-1 text-gray sm:text-lg border border-gray-300 px-3 py-1 rounded-full hover:bg-gray-50 transition-colors focus:bg-gray-100 focus:outline-none focus-visible:border-gray-500'>
            <span>Read more</span>
        </button>
    </a>

</div>